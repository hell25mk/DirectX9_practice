#pragma once

#include <Windows.h>
#include <tchar.h>

/*class Window final{

public:
	Window();
	~Window() = default;
	
	bool OriginalCreateWindow();
	bool ProcessLoop();

	void SetWindowSize(const int wid, const int hig);
	void SetClassName(_TCHAR* const name);
	void SetWindowName(_TCHAR* const name);

private:
	int widthSize;
	int heightSize;
	_TCHAR* className;
	_TCHAR* windowName;

};

LRESULT CALLBACK WndProc(HWND hWnd, UINT msg, WPARAM wParam, LPARAM lParam);*/